<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.everyfarm.board.ReviewDAO">
	<select id="list" resultType="kr.co.everyfarm.board.ReviewVO">
		SELECT * FROM review 
	</select>
	
	<insert id="insert" parameterType="kr.co.everyfarm.board.ReviewVO">
		INSERT INTO review ( rev_Id, rev_Title, rev_Cont, rev_Image, rev_Rate)
		values ( #{rev_Id}, #{rev_Title}, #{rev_Cont}, #{rev_Image}, #{rev_Rate})
	</insert>
	
	<select id="info" resultType="kr.co.everyfarm.board.ReviewVO">
		select * from emp2 where empno=#{empno}
	</select>
	
	<!-- 삭제할때  
	alter table board auto_increment =1 사용 할듯.-->
	<delete id="delete">
		delete from emp2 where empno=#{empno}
	</delete>
</mapper>