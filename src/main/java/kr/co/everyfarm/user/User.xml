<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.everyfarm.user.UserDAO">

	<select id="mlist" resultType="kr.co.everyfarm.user.MemberVO">
		select * from member
	</select>

	<select id="flist" resultType="kr.co.everyfarm.user.FarmerVO">
		select * from farmer
	</select>

	<insert id="mjoin" parameterType="kr.co.everyfarm.user.MemberVO">
		insert into member (m_id, m_name, m_pw, m_Addr, m_tel, m_birth, m_profile)
		Values (#{m_Id}, #{m_Name}, #{m_Pw}, #{m_Addr}, #{m_Tel}, #{m_Birth}, #{m_Profile})
	</insert>

	<insert id="fjoin" parameterType="kr.co.everyfarm.user.FarmerVO">
		insert into farmer (f_id,
		f_name, f_pw, f_Addr, f_tel, f_birth,
		f_profile, f_auth)
		Values (#{f_Id}, #{f_Name}, #{f_Pw}, #{f_Addr}, #{f_Tel}, #{f_Birth}, #{f_Profile}, #{f_Auth})
	</insert>

	<select id="mlogin" resultType="kr.co.everyfarm.user.MemberVO">
		select * from member where m_id =
		#{m_Id} and m_pw = #{m_Pw}
	</select>

	<select id="flogin" resultType="kr.co.everyfarm.user.FarmerVO">
		select * from farmer where f_id =
		#{f_Id} and f_pw = #{f_Pw}
	</select>

	<select id="mInfo" resultType="kr.co.everyfarm.user.MemberVO">
		select * from member where
		m_id=#{m_Id}
	</select>

	<select id="fInfo" resultType="kr.co.everyfarm.user.FarmerVO">
		select * from member where
		f_id=#{f_Id}
	</select>

	<update id="mUpdate">
		update member set
		m_Pw=#{m_Pw},
		m_Name=#{m_Name},
		m_subAddr=#{m_Subaddr},
		m_mainAddr=#{m_Mainaddr},
		m_detailAddr=#{m_Detailaddr},
		m_tel=#{m_Tel},
		m_birth=#{m_Birth},
		m_profile=#{m_Profile},
		where m_id=#{m_Id}
	</update>

	<update id="fUpdate">
		update farmer set
		f_Pw=#{f_Pw},
		f_Name=#{f_Name},
		f_subAddr=#{f_Subaddr},
		f_mainAddr=#{f_Mainaddr},
		f_detailAddr=#{f_Detailaddr},
		f_tel=#{f_Tel},
		f_birth=#{f_Birth},
		f_profile=#{f_Profile},
		f_auth=#{f_Auth},
		where f_id=#{f_Id}
	</update>

	<delete id="mDelete">
		delete from member where m_id=#{m_Id}
	</delete>

	<delete id="fDelete">
		delete from farmer where f_id=#{f_Id}
	</delete>
	
</mapper>